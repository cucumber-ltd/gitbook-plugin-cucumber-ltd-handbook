A [GitBook plugin](http://toolchain.gitbook.com/plugins/) for our handbook.

Adds functions to make it easier to query the set of pages and generate links 
between related documents based on their frontmatter.
